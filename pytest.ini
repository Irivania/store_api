[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
# Impede o paralelismo que causa fechamento prematuro do loop
# (Se precisar de paralelismo, use a flag -n no comando, mas agora deixe sem)
